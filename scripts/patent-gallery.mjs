import fs from 'node:fs';
const patents=JSON.parse(fs.readFileSync(new URL('../content/patent-certificates.json',import.meta.url),'utf8'));
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
export function patentGallery(){return `<section class="section patent-gallery" id="patent-certificates" aria-labelledby="certificate-heading"><div class="container"><div class="eyebrow">PATENT CERTIFICATES</div><h2 id="certificate-heading">특허증</h2><p>기운찬의 등록 특허증을 확인할 수 있습니다.</p><div class="certificate-grid">${patents.map(p=>`<article class="certificate-card"><button type="button" class="certificate-open" data-number="${esc(p.number)}" data-title="${esc(p.title)}" aria-label="${esc(p.country+' '+p.number+' '+p.title+' 특허증 보기')}" aria-haspopup="dialog" aria-controls="certificate-dialog"><span class="certificate-thumbnail"><img src="/assets/${p.image}" alt="${esc(p.country+' '+p.number+' '+p.title)}" width="${p.width}" height="${p.height}" loading="lazy"></span><span class="certificate-country">${p.country}</span><strong class="certificate-number">${esc(p.number)}</strong><span class="certificate-title">${esc(p.title)}</span><span class="certificate-action">특허증 보기 <span aria-hidden="true">↗</span></span></button></article>`).join('')}</div></div></section><dialog id="certificate-dialog" class="certificate-dialog" aria-labelledby="certificate-dialog-title" aria-describedby="certificate-dialog-number"><div class="certificate-dialog-inner"><div class="certificate-toolbar"><div><p id="certificate-dialog-number"></p><h2 id="certificate-dialog-title">특허증</h2></div><button type="button" class="certificate-close" aria-label="특허증 닫기" autofocus>닫기 ×</button></div><div class="certificate-tools"><button type="button" class="certificate-zoom" aria-pressed="false">원본 크기로 확대</button><a class="certificate-original" target="_blank" rel="noopener">원본 새 탭 보기 ↗</a></div><div class="certificate-viewer" tabindex="0" role="region" aria-label="특허증 이미지, 확대 시 스크롤하여 확인"><img class="certificate-full" alt=""></div></div></dialog>`;}
