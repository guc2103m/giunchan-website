import {publishedInsights} from '@/lib/content-data';import {siteOrigin} from '@/lib/site-data';
const esc=(s:string)=>s.replace(/[<>&'"]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;',"'":'&apos;','"':'&quot;'}[c]!));
export function GET(){const items=publishedInsights.map(x=>`<item><title>${esc(x.title)}</title><link>${siteOrigin}/insight/${x.slug}</link><guid>${siteOrigin}/insight/${x.slug}</guid><pubDate>${new Date(x.publishedAt).toUTCString()}</pubDate><description>${esc(x.summary)}</description></item>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>기운찬 인사이트</title><link>${siteOrigin}/insight</link><description>GMK®와 버섯균사체 지식 아카이브</description>${items}</channel></rss>`,{headers:{'Content-Type':'application/rss+xml; charset=utf-8'}})}
