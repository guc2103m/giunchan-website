import {publishedNews} from '@/lib/content-data';import {siteOrigin} from '@/lib/site-data';
const esc=(s:string)=>s.replace(/[<>&'"]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;',"'":'&apos;','"':'&quot;'}[c]!));
export function GET(){const items=publishedNews.map(x=>`<item><title>${esc(x.title)}</title><link>${siteOrigin}/newsroom/${x.slug}</link><guid>${siteOrigin}/newsroom/${x.slug}</guid><pubDate>${new Date(x.publishedAt).toUTCString()}</pubDate><description>${esc(x.summary)}</description></item>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>기운찬 뉴스룸</title><link>${siteOrigin}/newsroom</link><description>기운찬 연구·특허·제품·사업 소식</description>${items}</channel></rss>`,{headers:{'Content-Type':'application/rss+xml; charset=utf-8'}})}
